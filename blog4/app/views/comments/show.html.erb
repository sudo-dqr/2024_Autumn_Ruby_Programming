<p style="color: green"><%= notice %></p>

<%= render @comment %>

<div>
  <%= link_to "Edit this comment", edit_blog_comment_path(@blog, @comment) %> |
  <%= link_to "Back to comments", blog_comments_path(@blog) %>

  <%= button_to "Destroy this comment", blog_comment_path(@blog, @comment), method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
